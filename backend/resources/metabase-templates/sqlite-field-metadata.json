{
  "postings": {
    "transaction_date": {
      "coercion_strategy": "Coercion/ISO8601->DateTime",
      "description": "Transaction date in ISO8601 format (YYYY-MM-DD)"
    }
  },
  "postings_view": {
    "transaction_date": {
      "coercion_strategy": "Coercion/ISO8601->DateTime",
      "description": "Transaction date in ISO8601 format (YYYY-MM-DD)"
    }
  }
}
