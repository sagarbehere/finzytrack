{
  "schemaVersion": 2,
  "id": "financial-overview",
  "title": "Financial Overview",
  "description": "Overview of your financial status with net worth, assets, liabilities, and breakdowns",
  "layout": {
    "columns": 12,
    "gap": "1.5rem",
    "rowHeight": "140px",
    "widgets": [
      {
        "widgetId": "net-worth",
        "gridArea": "1 / 1 / 2 / 5"
      },
      {
        "widgetId": "total-assets",
        "gridArea": "1 / 5 / 2 / 9"
      },
      {
        "widgetId": "total-liabilities",
        "gridArea": "1 / 9 / 2 / 13"
      },
      {
        "widgetId": "assets-pie",
        "gridArea": "2 / 1 / 5 / 7"
      },
      {
        "widgetId": "liabilities-pie",
        "gridArea": "2 / 7 / 5 / 13"
      }
    ]
  },
  "widgets": [
    {
      "id": "net-worth",
      "title": "Net Worth",
      "visualization": {
        "type": "kpi",
        "icon": "$",
        "multiCurrency": true
      },
      "steps": [
        {
          "id": "main",
          "kind": "query",
          "query": "SELECT currency, SUM(amount) AS amount FROM (SELECT p.currency AS currency, SUM(CAST(p.amount AS REAL)) AS amount FROM postings p JOIN commodities c ON p.currency = c.code WHERE c.is_currency = 1 AND p.account_type IN ('Assets', 'Liabilities') GROUP BY p.currency UNION ALL SELECT lp.quote_currency AS currency, SUM(CAST(l.units_number AS REAL) * lp.price) AS amount FROM lots l JOIN commodities c ON c.code = l.units_currency AND c.is_currency = 0 JOIN (SELECT base_currency, quote_currency, CAST(quote_number AS REAL) AS price FROM prices x WHERE x.date = (SELECT MAX(date) FROM prices y WHERE y.base_currency = x.base_currency AND y.quote_currency = x.quote_currency)) lp ON lp.base_currency = l.units_currency GROUP BY lp.quote_currency) GROUP BY currency HAVING amount != 0 ORDER BY currency"
        },
        {
          "id": "out",
          "kind": "transform",
          "fn": "none",
          "inputs": [
            "{{steps.main}}"
          ]
        }
      ],
      "output": "out"
    },
    {
      "id": "total-assets",
      "title": "Total Assets",
      "visualization": {
        "type": "kpi",
        "icon": "↑",
        "iconColor": "{{theme.brand}}",
        "multiCurrency": true
      },
      "steps": [
        {
          "id": "main",
          "kind": "query",
          "query": "SELECT currency, SUM(amount) AS amount FROM (SELECT p.currency AS currency, SUM(CAST(p.amount AS REAL)) AS amount FROM postings p JOIN commodities c ON p.currency = c.code WHERE c.is_currency = 1 AND p.account_type = 'Assets' GROUP BY p.currency UNION ALL SELECT lp.quote_currency AS currency, SUM(CAST(l.units_number AS REAL) * lp.price) AS amount FROM lots l JOIN commodities c ON c.code = l.units_currency AND c.is_currency = 0 JOIN (SELECT base_currency, quote_currency, CAST(quote_number AS REAL) AS price FROM prices x WHERE x.date = (SELECT MAX(date) FROM prices y WHERE y.base_currency = x.base_currency AND y.quote_currency = x.quote_currency)) lp ON lp.base_currency = l.units_currency GROUP BY lp.quote_currency) GROUP BY currency HAVING amount != 0 ORDER BY currency"
        },
        {
          "id": "out",
          "kind": "transform",
          "fn": "none",
          "inputs": [
            "{{steps.main}}"
          ]
        }
      ],
      "output": "out"
    },
    {
      "id": "total-liabilities",
      "title": "Total Liabilities",
      "visualization": {
        "type": "kpi",
        "icon": "↓",
        "iconColor": "{{theme.brand}}",
        "multiCurrency": true
      },
      "steps": [
        {
          "id": "main",
          "kind": "query",
          "query": "SELECT currency, SUM(CAST(amount AS REAL)) * -1 AS amount FROM postings p JOIN commodities c ON p.currency = c.code WHERE c.is_currency = 1 AND account_type = 'Liabilities' GROUP BY currency HAVING amount != 0 ORDER BY currency"
        },
        {
          "id": "out",
          "kind": "transform",
          "fn": "none",
          "inputs": [
            "{{steps.main}}"
          ]
        }
      ],
      "output": "out"
    },
    {
      "id": "assets-pie",
      "title": "Assets Breakdown",
      "helpText": "Only shows accounts with a positive balance. Accounts with negative balances (e.g. overdrawn) are excluded since pie charts cannot display negative values.",
      "parameters": [
        {
          "name": "currency",
          "label": "Currency",
          "type": "select",
          "default": {
            "$gen": "defaultCurrency"
          },
          "optionsFrom": "currencies"
        }
      ],
      "visualization": {
        "type": "chart",
        "chartType": "pie",
        "options": {
          "tooltip": {
            "trigger": "item"
          },
          "legend": {
            "show": false
          },
          "series": [
            {
              "type": "pie",
              "radius": [
                "35%",
                "65%"
              ],
              "center": [
                "50%",
                "50%"
              ],
              "encode": {
                "itemName": "name",
                "value": "value"
              },
              "label": {
                "show": true,
                "formatter": "{b}",
                "minShowLabelAngle": 15
              },
              "labelLine": {
                "show": true,
                "length": 8,
                "length2": 8
              },
              "emphasis": {
                "label": {
                  "show": true,
                  "fontWeight": "bold",
                  "formatter": "{b}\n{d}%"
                }
              }
            }
          ]
        },
        "clickLink": {
          "name": "transactions",
          "query": {
            "accountContains": "{{data.account}}"
          }
        }
      },
      "steps": [
        {
          "id": "main",
          "kind": "query",
          "query": "SELECT CASE WHEN account LIKE 'Assets:Liquid:%' THEN REPLACE(account, 'Assets:Liquid:', '') WHEN account LIKE 'Assets:Investments:%' THEN REPLACE(account, 'Assets:Investments:', '') ELSE REPLACE(account, 'Assets:', '') END AS name, account, ROUND(SUM(value), 2) AS value FROM (SELECT p.account AS account, SUM(CAST(p.amount AS REAL)) AS value FROM postings p JOIN commodities c ON p.currency = c.code WHERE p.account_type = 'Assets' AND c.is_currency = 1 AND p.currency = :currency GROUP BY p.account UNION ALL SELECT l.account AS account, SUM(CAST(l.units_number AS REAL) * lp.price) AS value FROM lots l JOIN commodities c ON c.code = l.units_currency AND c.is_currency = 0 JOIN (SELECT base_currency, quote_currency, CAST(quote_number AS REAL) AS price FROM prices x WHERE x.date = (SELECT MAX(date) FROM prices y WHERE y.base_currency = x.base_currency AND y.quote_currency = x.quote_currency)) lp ON lp.base_currency = l.units_currency AND lp.quote_currency = :currency GROUP BY l.account) GROUP BY account HAVING value > 0 ORDER BY value DESC"
        },
        {
          "id": "out",
          "kind": "transform",
          "fn": "none",
          "inputs": [
            "{{steps.main}}"
          ]
        }
      ],
      "output": "out"
    },
    {
      "id": "liabilities-pie",
      "title": "Liabilities Breakdown",
      "helpText": "Only shows accounts with outstanding balances. Accounts with negative balances (e.g. overpayment or refund) are excluded since pie charts cannot display negative values.",
      "parameters": [
        {
          "name": "currency",
          "label": "Currency",
          "type": "select",
          "default": {
            "$gen": "defaultCurrency"
          },
          "optionsFrom": "currencies"
        }
      ],
      "visualization": {
        "type": "chart",
        "chartType": "pie",
        "options": {
          "tooltip": {
            "trigger": "item"
          },
          "legend": {
            "show": false
          },
          "series": [
            {
              "type": "pie",
              "radius": [
                "35%",
                "65%"
              ],
              "center": [
                "50%",
                "50%"
              ],
              "encode": {
                "itemName": "name",
                "value": "value"
              },
              "label": {
                "show": true,
                "formatter": "{b}",
                "minShowLabelAngle": 15
              },
              "labelLine": {
                "show": true,
                "length": 8,
                "length2": 8
              },
              "emphasis": {
                "label": {
                  "show": true,
                  "fontWeight": "bold",
                  "formatter": "{b}\n{d}%"
                }
              }
            }
          ]
        },
        "clickLink": {
          "name": "transactions",
          "query": {
            "accountContains": "{{data.account}}"
          }
        }
      },
      "steps": [
        {
          "id": "main",
          "kind": "query",
          "query": "SELECT REPLACE(account, 'Liabilities:', '') AS name, account, ROUND(SUM(CAST(amount AS REAL)) * -1, 2) AS value FROM postings WHERE account_type = 'Liabilities' AND currency = :currency GROUP BY account HAVING value > 0 ORDER BY value DESC"
        },
        {
          "id": "out",
          "kind": "transform",
          "fn": "none",
          "inputs": [
            "{{steps.main}}"
          ]
        }
      ],
      "output": "out"
    }
  ]
}
