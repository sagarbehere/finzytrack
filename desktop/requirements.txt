pyinstaller>=6.0.0
pywebview==6.2.1

# pywebview on Windows pulls in pythonnet + clr_loader transitively.
# Pin them explicitly so dev and CI install identical binaries — version
# drift in this stack has produced "Failed to resolve Python.Runtime
# .Loader.Initialize" errors that only reproduced on one machine.
pythonnet==3.1.0; sys_platform == "win32"
clr_loader==0.3.1; sys_platform == "win32"

# Icon generation (assets/icons/generate.py rasterises master.svg)
Pillow>=10.0.0

# PyWebView GTK backend (Linux only).
# Pinned to <3.51 because newer PyGObject crashes pywebview's on_load_finish
# callback with "TypeError: Must be number, not method". Remove the pin once
# pywebview's GTK backend is updated for the newer gi binding.
pygobject<3.51; sys_platform == "linux"
pycairo; sys_platform == "linux"
